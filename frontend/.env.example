# По умолчанию клиент использует тот же origin и префикс из OpenAPI.
VITE_API_BASE_URL=/api/v1

# Используется только dev-сервером Vite; production оставляет same-origin API.
VITE_API_PROXY_TARGET=http://127.0.0.1:5050
